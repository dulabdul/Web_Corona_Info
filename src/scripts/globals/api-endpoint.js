import CONFIG from './config';
const API_ENDPOINT = {
  INDONESIA: `${CONFIG.BASE_URL}countries/indonesia`,
  VAKSIN: `${CONFIG.BASE_API}`,
  PROVINSI: `${CONFIG.PROV}`,
};

export default API_ENDPOINT;
